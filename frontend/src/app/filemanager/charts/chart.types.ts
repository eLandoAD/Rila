export interface DonutSegment {
  label: string;
  value: number;
  color: string;
}

export interface BarDatum {
  label: string;
  value: number;
  color?: string;
}

export interface LinePoint {
  label: string;
  value: number;
}
